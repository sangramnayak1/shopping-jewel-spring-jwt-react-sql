spring.application.name=shopping-jewel
spring.datasource.url=jdbc:mysql://localhost:3306/shopping_cart?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=shopuser
spring.datasource.password=shoppass
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect


server.port=8080

# Expose only the health endpoint
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=always
#management.endpoint.health.probes.enabled=true

# Allow health endpoint without authentication
spring.security.filter.dispatcher-types=ASYNC,ERROR,REQUEST

# JWT
jwt.secret=3txBv4w/zqWBKY5zHHT7p4kx547vqixR/yH5jUTHScY=
jwt.expiration=3600000

# SMTP placeholders (not active — EmailService is mock)
mail.host=your-smtp-host
mail.port=587
mail.username=your-username
mail.password=your-password
mail.from=no-reply@example.com

# OTP settings
otp.expiration.minutes=10
